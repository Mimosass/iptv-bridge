# 使用步骤（从零到能看，一步一步来）

这份文档假设你**从没用过 GitHub**。跟着做，大约 15 分钟。

最终效果：你得到一个永久不变的订阅链接，SenPlayer 填一次就够，**以后源自动更新，你什么都不用管**。

---

## 先把压缩包解开

解压后你会看到这些文件：

```
iptv-bridge/
├── .github/
│   └── workflows/
│       └── sync.yml      ← 自动更新的定时器
├── scripts/
│   └── build.py          ← 抓取并转换的脚本
├── config.json           ← 你想订哪些源，写在这里
└── README.md
```

> ⚠️ 注意 `.github` 是**隐藏文件夹**（点号开头）。Windows 资源管理器默认显示它；macOS 访达里默认看不见，按 `Shift + Command + .` 可以切换显示。

---

# 方案 A：GitHub 全自动（推荐）

## 第 1 步：注册 / 登录 GitHub

打开 https://github.com ，有账号就直接登录，没有就注册一个（免费，只需要邮箱）。

---

## 第 2 步：新建一个仓库

1. 登录后，点右上角头像左边的 **`+`** → **New repository**
2. 按下面填：

| 字段 | 填什么 |
| --- | --- |
| Repository name | `iptv-bridge`（随便起，记住它，后面要用到） |
| Description | 随便写，可留空 |
| **Public** | ✅ **必须选这个**（重要，见下方说明） |
| Add a README file | ❌ **不要勾**（我们已经有了） |
| Add .gitignore | ❌ 不要勾 |
| Choose a license | ❌ 不要勾 |

3. 点绿色按钮 **Create repository**

> 🔴 **为什么必须 Public？**
> 私有仓库的文件链接需要登录凭证才能访问，SenPlayer 拿不到，会一直加载失败。
> 这个仓库里只有源地址配置文件，不含任何你的个人信息，公开是安全的。

---

## 第 3 步：上传文件

建好仓库后，你会看到一个"Quick setup"页面，上面有仓库地址，形如：

```
https://github.com/你的用户名/iptv-bridge
```

**记住你的用户名**，后面拼链接要用。

### 方法 3.1：网页上传（不用装任何软件）

1. 在仓库页面，点 **Add file** → **Upload files**
2. 把解压出来的**所有文件**拖进上传框，包括 `.github` 文件夹
3. 往下滚，点绿色 **Commit changes**

> ⚠️ **容易踩的坑：`.github` 传不上去**
> 网页上传经常把这个点号开头的文件夹吞掉。如果传完发现仓库里没有 `.github`，用下面这个办法补救：
>
> 上传时，在文件名的输入框里**手动敲路径**。GitHub 的上传框支持直接输入 `a/b/c.txt` 这种带斜杠的名字来建子目录。
> 具体操作：单独再传一次 `sync.yml`，在文件名框里把 `sync.yml` 改成：
> ```
> .github/workflows/sync.yml
> ```
> 它会自动帮你建出两级目录。

### 方法 3.2：命令行上传（更稳，推荐如果会用）

```bash
cd iptv-bridge          # 进到解压出来的那个文件夹
git init
git add -A
git commit -m "init"
git branch -M main
git remote add origin https://github.com/你的用户名/iptv-bridge.git
git push -u origin main
```

传完刷新仓库页面，确认能看到这 4 个东西：`.github`、`scripts`、`config.json`、`README.md`。

---

## 第 4 步：打开 Actions 的写入权限

**这一步不做的话，自动更新会失败。**

1. 进仓库页面，点顶部的 **Settings**（⚙️ 齿轮图标）
2. 左侧菜单点 **Actions** → **General**
3. 往下滑到 **Workflow permissions**
4. 选 **Read and write permissions**
5. 点 **Save**

---

## 第 5 步：手动跑第一次

自动任务是每 6 小时一次，第一次别等，手动触发：

1. 点仓库顶部的 **Actions** 标签页
2. 如果看到提示 "Workflows aren't being run on this fork" 或 "I understand my workflows, go ahead and enable them"，**点那个绿色按钮启用**
3. 左侧工作流列表里点 **更新订阅**
4. 右上角点 **Run workflow** → 再点一次绿色的 **Run workflow**
5. 等大约 10 秒，列表里会出现一条正在跑的任务（黄色圆点 🟡）
6. 点进去能看到实时日志。等圆点变成**绿色 ✅**（通常 20~60 秒）

> 如果是红色 ❌，点进去看红色报错行，最常见的原因是第 3 步的 `.github` 没传上去，或者第 4 步权限没开。

---

## 第 6 步：确认文件已生成

跑完后回到仓库首页（**Code** 标签），你会看到多出来一个 `dist` 文件夹，里面应该有 4 个文件：

```
dist/
├── live.m3u      ← SenPlayer 要订阅的就是这个
├── live.txt      ← 备选
├── tvbox.json    ← TVBox 用的，SenPlayer 用不上
└── README.md     ← 本次抓取的统计报告
```

点开 `live.m3u` 看一眼，内容应该长这样：

```
#EXTM3U
#EXTINF:-1 tvg-name="CCTV-1" group-title="iptv-org 中国大陆",CCTV-1
http://xxx.xxx.com/live/cctv1.m3u8
```

看到这个就说明成功了。

---

## 第 7 步：拼出你的订阅链接

把下面这行的三个占位符替换掉（**注意大小写完全照抄你的仓库信息**）：

```
https://raw.githubusercontent.com/你的用户名/仓库名/分支名/dist/live.m3u
```

- **你的用户名**：仓库 URL 里 `github.com/` 后面那一段
- **仓库名**：`iptv-bridge`
- **分支名**：去仓库首页看，代码块上方那个下拉框写的是 `main` 还是 `master`（新仓库默认是 `main`）

举例，假如用户名叫 `zhangsan`：

```
https://raw.githubusercontent.com/zhangsan/iptv-bridge/main/dist/live.m3u
```

### 验证链接是否有效

**把这串地址粘到电脑浏览器地址栏里打开。** 如果浏览器显示出一堆 `http://...m3u8` 的文本，说明链接是对的。如果显示 404，回去检查用户名、仓库名、分支名有没有拼错。

---

## 第 8 步：填进 SenPlayer

1. 打开 **SenPlayer**
2. 找到 **IPTV / 直播** 入口
3. 选 **添加订阅 / 添加播放列表 / 网络导入**（不同版本措辞略有差异）
4. 粘贴刚才那串链接
5. 确认，等待解析

导入完成后应该能看到按分组排列的频道列表，点一个试试。

---

## 第 9 步：确认自动更新生效

不需要额外配置，`sync.yml` 里已经写好了每 6 小时自动跑一次（北京时间 2:00 / 8:00 / 14:00 / 20:00）。

想验证的话：过一天后去仓库的 **Actions** 页面，应该能看到好几条绿色的运行记录。也可以在 **Code** 页面点 `dist/README.md`，里面有每次运行的时间戳。

**你以后想加源、删源，只需要改 `config.json` 然后 push，Actions 会自动重新生成。**

---

# 国内打不开 GitHub？换 CDN 链接

`raw.githubusercontent.com` 在国内经常连不上。换成 jsDelivr（免费 CDN）：

```
https://cdn.jsdelivr.net/gh/你的用户名/仓库名@分支名/dist/live.m3u
```

举例：

```
https://cdn.jsdelivr.net/gh/zhangsan/iptv-bridge@main/dist/live.m3u
```

注意事项：

- jsDelivr 对单个文件限制 20MB，我们的文件才 100KB 左右，远远够用
- **新仓库第一次使用 jsDelivr 需要等几分钟到几小时同步**，之后就快了
- jsDelivr **有约 12 小时缓存**。你改完 `config.json`，CDN 上的内容不会立刻变。想立刻生效，在浏览器里打开下面这个地址清一下缓存（把用户名仓库名换成你的）：

  ```
  https://purge.jsdelivr.net/gh/你的用户名/仓库名@main/dist/live.m3u
  ```

- 页面显示 `{"status":"ok"}` 就说明缓存清掉了

**建议：SenPlayer 里填 jsDelivr 链接，稳。**

---

# 方案 B：不想用 GitHub

如果你实在不想注册 GitHub，有两个替代路线。

## B1：Gitee（国内码云，速度快）

1. 注册 https://gitee.com
2. 新建**公开**仓库，把文件上传上去
3. 手动或用 Gitee 的流水线生成 `dist/live.m3u`（Gitee 免费版没有 GitHub Actions，但可以用 Gitee Go，或者你本地跑一次脚本再传上去）
4. 订阅链接格式：

   ```
   https://gitee.com/你的用户名/仓库名/raw/分支名/dist/live.m3u
   ```

缺点：自动更新需要额外折腾，且 Gitee 对公开仓库有审核。

## B2：本地跑 + 局域网共享（最可控）

适合你有一台常开的电脑 / NAS。

1. 装好 Python 3（macOS 和 Linux 自带；Windows 去 python.org 下）
2. 定时跑脚本：

   ```bash
   cd iptv-bridge
   python3 scripts/build.py
   ```

3. 在 `dist` 目录起一个静态服务：

   ```bash
   cd dist && python3 -m http.server 8000
   ```

4. SenPlayer 里填：

   ```
   http://电脑的局域网IP:8000/live.m3u
   ```

   比如 `http://192.168.1.100:8000/live.m3u`

5. 把第 2 步配成定时任务：
   - **macOS / Linux**：`crontab -e` 加一行，每天凌晨 4 点跑一次
     ```
     0 4 * * * cd /path/to/iptv-bridge && python3 scripts/build.py
     ```
   - **Windows**：任务计划程序，触发器选"每天"，操作填 `python.exe`，参数填脚本路径

缺点：电脑关机就断，且只能在家里用。

---

# 常见问题

**Q：SenPlayer 里提示"解析失败"或列表是空的**

按顺序排查：
1. 把订阅链接粘到**电脑浏览器**里打开，看能不能显示文本。打不开就是链接错了（通常是分支名 `main`/`master` 搞混）
2. 浏览器能开但 SenPlayer 不行 → 换 jsDelivr 链接
3. 还是不行 → 换 `live.txt` 试试（有些版本对 txt 兼容性更好）

**Q：能加载频道，但点开播不了**

正常的。公开直播源失效是常态，这也是为什么要每频道保留 3 条备用线路——**同一个频道名下面有多条，一条挂了会自动切下一条**。如果某个频道全部线路都挂了，等下次自动更新（最多 6 小时）。

**Q：频道太多 / 想只看央视卫视**

编辑 `config.json`：

```json
"live_include_keywords": ["CCTV", "卫视"]
```

这样只保留频道名里含这些词的。想过滤掉某些词（比如成人内容）用：

```json
"live_exclude_keywords": ["色情", "成人", "XXX"]
```

改完 push，Actions 会自动重新生成。用 jsDelivr 的话记得 purge 缓存。

**Q：想加自己的源**

在 `config.json` 的 `live_sources` 数组里加一项：

```json
{ "name": "我自己的源", "url": "https://example.com/list.m3u" }
```

脚本支持三种格式：标准 M3U、TXT 的 `频道名,地址`、TXT 的 `地址,频道名`。

**Q：Actions 报错 / 一直是红色**

90% 是两个原因：
- 第 3 步的 `.github/workflows/sync.yml` 没传上去 → 回仓库确认文件在
- 第 4 步的写入权限没开 → 重新检查一次

**Q：会泄露我的隐私吗**

不会。仓库里只有公开的源地址配置文件，没有任何你的个人信息、观看记录或设备信息。

---

# 一句话总结

| 你想干什么 | 怎么做 |
| --- | --- |
| 换源 / 加源 | 改 `config.json`，push |
| 立刻生效 | Actions 页面手动 Run workflow；用 jsDelivr 的话再 purge 缓存 |
| 平时维护 | **什么都不用做**，每 6 小时自动跑 |
| 订阅地址 | `https://cdn.jsdelivr.net/gh/用户名/仓库名@main/dist/live.m3u` |
